[package]
name = "ultrahonk_rust_verifier"
version = "0.1.0"
edition = "2021"

license     = "MIT"
description = "Rust verifier for UltraHonk proofs"
repository  = "https://github.com/yugocabrio/ultrahonk-rust-verifier"

[dependencies]
sha3 = { version = "0.10", default-features = false }

# Only ark-ff and ark-bn254 needed for Fr scalar field arithmetic
ark-ff = { version = "0.5", default-features = false }
ark-bn254 = { version = "0.5", default-features = false, features = ["curve"] }

serde = { version = "1.0", features = ["derive"], default-features = false, optional = true }
serde_json = { version = "1.0", default-features = false, features = ["alloc"], optional = true }
hex = { version = "0.4", default-features = false, features = ["alloc"] }

lazy_static = { version = "1.4", optional = true }

[features]
default = []
# Soroban BN254 precompile integration switch (no allocator needed)
soroban-precompile = []
std = [
    "sha3/std",
    "ark-ff/std",
    "ark-bn254/std",
    "hex/std",
    "lazy_static",
    "alloc",
]
trace = []

# Optional alloc support (not required for core functionality)
alloc = [
    "hex/alloc",
]
